1. user can create transactions, or delete or edit them
2. user can view his transactions
3. user can see the budget it is linked to and wallet, so put them in a column of the table with the transactions
4. user can see the date, amount and type of the transaction
5. user can see the description of the transaction
6. user can see the actions of the transaction, edit, delete

user can see the top ten transactions of the selected budget
if not budget is selected, then all transactions are shown
then user can see a line chart of the transactions.
filters are by date, wallet, id, category.

if user clicked on a wallet, then the transactions of that wallet are shown.